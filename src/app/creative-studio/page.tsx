import { AppShell } from "@/components/app-shell";
import { ModulePage } from "@/components/module-page";

export default function CreativeStudioPage() {
  return (
    <AppShell>
      <ModulePage title="Creative Studio" description="Draft-only creative workspace for later product, offer, audience, platform, funnel stage, compliance, and approval workflows." />
    </AppShell>
  );
}
