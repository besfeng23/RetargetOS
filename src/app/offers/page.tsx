import { AppShell } from "@/components/app-shell";
import { ModulePage } from "@/components/module-page";

export default function OffersPage() {
  return (
    <AppShell>
      <ModulePage title="Offers" description="Offer library foundation for products, affiliate offers, merchant campaigns, margins, payout, refund risk, and audience fit." />
    </AppShell>
  );
}
